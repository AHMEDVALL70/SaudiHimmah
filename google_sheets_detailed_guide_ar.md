# دليل استخدام Google Sheets لإدارة عروض SaudiHimmah

## أولًا: فتح Google Sheets على جهاز Mac

لا تحتاج إلى تثبيت برنامج على جهاز Mac. افتح Safari أو Chrome، ثم انتقل إلى:

[https://sheets.google.com](https://sheets.google.com)

سجّل الدخول بحساب Google، ثم اضغط **جدول بيانات فارغ** لإنشاء جدول جديد. ويمكنك أيضًا استخدام تطبيق Google Sheets من متجر التطبيقات إذا كان متاحًا في بلدك، لكن استخدام المتصفح يكفي تمامًا.

## ثانيًا: إنشاء ورقة العروض

غيّر اسم الملف إلى `SaudiHimmah - العروض العقارية`، ثم غيّر اسم التبويب السفلي إلى `العروض العقارية`.

في الصف الأول اكتب أسماء الأعمدة التالية. من المهم كتابة الأسماء كما هي حتى يتعرف عليها الموقع دون أخطاء:

| رقم العمود | اسم العمود | إلزامي؟ | مثال |
|---:|---|---|---|
| A | المدينة | نعم | الرياض |
| B | الحي | نعم | الندى |
| C | النوع | نعم | شقة في عمارة |
| D | السعر | نعم | 1480000 |
| E | المساحة | نعم | 257 |
| F | حالة الإعلان | مستحسن | متاحة للبيع |
| G | العمر | مستحسن | 1 |
| H | الغرف | مستحسن | 3 |
| I | الواجهة | مستحسن | شمالية |
| J | عرض الشارع | مستحسن | 20 |
| K | رابط الخريطة | مستحسن | https://maps.app.goo.gl/... |
| L | الوصف | مستحسن | شقة مع سطح خاص |
| M | رقم الإعلان | اختياري | 7526170 |
| N | رقم الرخصة | اختياري | 55555555 |
| O | الترخيص الإعلاني | اختياري | 44444444 |
| P | عدد دورات المياه | اختياري | 3 |
| Q | المميزات | اختياري | مؤثثة، تكييف راكب، موقفان |
| R | رابط الصورة | اختياري | https://example.com/image.jpg |
| S | تاريخ التحديث | اختياري | 2026-08-31 |

يجب أن يكون الصف الأول للعناوين فقط، ويبدأ أول عرض من الصف الثاني. لا تترك صفوفًا فارغة بين العروض، ولا تضع رموز العملة داخل خانة السعر. اكتب السعر رقمًا مثل `1480000` وليس `1,480,000 ريال`.

## ثالثًا: مثال عرض مكتمل

| المدينة | الحي | النوع | السعر | المساحة | حالة الإعلان | العمر | الغرف | الواجهة | عرض الشارع | رابط الخريطة | الوصف |
|---|---|---|---:|---:|---|---:|---:|---|---:|---|---|
| الرياض | الندى | شقة في عمارة | 1480000 | 257 | متاحة للبيع | 1 | 3 | شمالية | 20 | https://maps.app.goo.gl/H8nTsdiuQSD58rgo9?g_st=ic | شقة مع سطح خاص في مشروع سيل |

لعرض عقار تم بيعه، اكتب في عمود **حالة الإعلان**: `تم البيع`. ولعرض عقار متاح، اكتب: `متاحة للبيع`. يفضل توحيد الصياغة نفسها في جميع الصفوف.

## رابعًا: المدن والأحياء

استخدم أسماء المدن التي يدعمها الموقع حاليًا، مثل:

`المدينة المنورة`، `مكة المكرمة`، `جدة`، `الرياض`، و`الدمام`.

اكتب اسم الحي في عمود `الحي` كما يظهر في قائمة الموقع. إذا أضفت مدينة أو حيًا جديدًا، يجب إضافته أيضًا إلى إعدادات المدن والأحياء في الموقع حتى يظهر في القوائم المنسدلة. أما مجرد كتابته في Google Sheets فلن ينشئ خيارًا جديدًا داخل قائمة التقييم تلقائيًا إلا بعد تطوير الربط ليقرأ المدن ديناميكيًا.

## خامسًا: تحديث البيانات من الجوال

نزّل تطبيق **Google Sheets** على هاتف iPhone أو Android، أو افتح الموقع من متصفح الهاتف. افتح الملف، ثم اضغط الخلية التي تريد تعديلها.

لإضافة عرض جديد، انتقل إلى أول صف فارغ واكتب بياناته في الأعمدة نفسها. لتغيير حالة عرض، عدّل الخلية في عمود `حالة الإعلان` من `متاحة للبيع` إلى `تم البيع`. لا تحذف عنوان العمود ولا تغيّر ترتيبه بعد ربط الموقع إلا بعد تحديث إعدادات الربط.

تُحفظ التعديلات تلقائيًا عادةً عند توفر الإنترنت. بعد التعديل، قد تحتاج الصفحة المنشورة إلى تحديث، وقد يتأخر ظهور التغييرات العامة بضع دقائق إذا استخدمت ميزة النشر على الويب.

## سادسًا: نشر الجدول للقراءة من الموقع

هذا الخيار مناسب للعروض العامة التي لا تحتوي على بيانات سرية:

1. افتح جدول Google Sheets من الكمبيوتر.
2. اختر **ملف**.
3. اختر **مشاركة**.
4. اختر **النشر على الويب**.
5. حدّد تبويب `العروض العقارية` بدل نشر كل الملف.
6. اختر صيغة `CSV` أو `صفحة ويب` بحسب طريقة الربط.
7. اضغط **نشر** ثم وافق على التأكيد.
8. انسخ رابط النشر وأرسله لي أو احتفظ به لاستخدامه في الموقع.

أي تعديل على الجدول الأصلي قد يظهر في النسخة المنشورة بعد فترة قصيرة، لكن لا تضع أرقام هويات أو عقودًا أو بيانات عملاء أو معلومات حساسة في جدول منشور للعامة.

## سابعًا: استخراج معرّف الجدول

إذا كان رابط الجدول بهذا الشكل:

```text
https://docs.google.com/spreadsheets/d/1AbCDefGHIjkLMNopQR/edit#gid=0
```

فإن معرّف الجدول هو الجزء الواقع بين `/d/` و`/edit`:

```text
1AbCDefGHIjkLMNopQR
```

يحتاج الموقع إلى هذا المعرّف أو إلى رابط تطبيق الويب حتى يقرأ البيانات تلقائيًا.

## ثامنًا: ربط Google Apps Script بالموقع

إذا أردت ربطًا أكثر تنظيمًا من رابط CSV، افتح الجدول ثم اختر **الإضافات > Apps Script**. الصق سكربت القراءة الذي أرسلته لك في الدليل السابق، ثم غيّر:

```javascript
const SHEET_ID = 'ضع_معرف_الجدول_هنا';
const SHEET_NAME = 'العروض العقارية';
```

بعد حفظ الكود، اختر **Deploy > New deployment > Web app**. اجعل التطبيق ينفّذ باسمك، ثم اختر مستوى الوصول المناسب، وانسخ الرابط الذي ينتهي بـ `/exec`. لا تستخدم رابط `/dev` للموقع المنشور؛ رابط `/dev` مخصص للاختبار.

بعد أن ترسل لي رابط `/exec`، أضيفه إلى الموقع وأحوّل الصفوف إلى بطاقات عروض، ونافذة تفاصيل، وحالات `متاحة للبيع` و`تم البيع`، وأستخدم السعر والمساحة والمدينة والحي في التقييم.

## تاسعًا: أخطاء شائعة يجب تجنبها

لا تكتب `الرياض ` بمسافة إضافية في بعض الصفوف و`الرياض` في صفوف أخرى. لا تخلط بين `مكة` و`مكة المكرمة` إذا كان الموقع يعتمد الاسم الكامل. لا تضع فواصل أو كلمة `ريال` داخل عمود السعر. لا تضع أكثر من عقار في صف واحد. لا تستخدم أسماء أعمدة مختلفة مثل `سعر العقار` بدل `السعر` قبل تعديل كود القراءة.

## عاشرًا: أفضل ترتيب للعمل

ابدأ بإضافة خمسة عروض تجريبية صحيحة فقط، ثم انشر الجدول، ثم اختبر القراءة من الموقع. بعد نجاح الاختبار، أضف بقية العروض. احتفظ بنسخة احتياطية من الجدول قبل الحذف أو التعديل الجماعي.

للاستخدام الحالي، الخيار العملي هو Google Sheets للعروض العامة. أما عند الحاجة إلى تسجيل دخول، صور خاصة، صلاحيات للوسطاء، بيانات العملاء، أو لوحة تحكم، فسيكون الانتقال إلى Supabase هو الخيار الأنسب لاحقًا، ويمكن نقل هذه الأعمدة نفسها إليه.

## مراجع Google الرسمية

[1]: https://support.google.com/docs/answer/183965?hl=ar "مساعدة Google: إتاحة جداول بيانات Google للجميع"

[2]: https://developers.google.com/apps-script/guides/web "Google Developers: تطبيقات الويب في Apps Script"

[3]: https://developers.google.com/workspace/sheets/api/guides/concepts "Google Developers: Google Sheets API Overview"
